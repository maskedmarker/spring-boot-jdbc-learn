
```text
对于 H2 数据库，可以直接在 JDBC URL 中添加参数来打印 SQL：

spring:
  datasource:
    url: jdbc:h2:tcp://localhost:9092/mem:test;TRACE_LEVEL_SYSTEM_OUT=2
    
    
TRACE_LEVEL_SYSTEM_OUT 参数说明：
- 0 = 关闭（默认）
- 1 = 只打印 SQL 语句
- 2 = 打印 SQL 和参数
- 3 = 打印详细信息（包括结果集）    
```